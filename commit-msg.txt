修复(day-3): 修复登录成功后无法跳转到工作台的问题

- 在 LoginView 中使用 await router.push，防止异步跳转异常被静默吞掉
- 安全解析 route.query.redirect，避免多值参数或登录页地址导致跳转循环
- 调整路由守卫中会话恢复的顺序，确保有 Token 即可提前初始化用户态
- 在 userStore.login 中提前标记 initialized，防止跳转期间重复触发 restoreSession